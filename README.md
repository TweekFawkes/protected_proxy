# protected_proxy
A protected proxy (like a reverse proxy or secure web proxy) is a gateway server that acts as an intermediary. It inspects, filters, and secures traffic between users and web applications. It hides backend infrastructure, blocks malicious requests, and provides centralized authentication. 
